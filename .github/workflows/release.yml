name: Release to PyPi

on: 
  workflow_dispatch:
  repository_dispatch:
    types: [release-python]

jobs:
  release-python:
    runs-on: [self-hosted, Linux]

    steps:
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      # see https://github.com/peter-evans/repository-dispatch
      # - name: Dispatch Payload
      #   run: echo ${{ github.event.client_payload }}
        
      - name: Upload to PyPi
        run: |
          ll
          
          source /__/devbox/.env.sh

          ll
          mkdir sling/bin

          cd sling/bin
          wget https://slingelt.n1sl.com/slingdata.io/dist/sling-linux
          wget https://slingelt.n1sl.com/slingdata.io/dist/sling-mac
          wget https://slingelt.n1sl.com/slingdata.io/dist/sling-win.exe
          cd -

          chmod -R 755 sling/bin

          pip install -e .
          pip install twine
          python setup.py sdist && twine upload --verbose --skip-existing dist/*